% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_titres.R
\name{miss_titres_random}
\alias{miss_titres_random}
\title{Remove titres to create random missing data}
\usage{
miss_titres_random(titre, proportion, keep_homologous = T, min_dim = NULL)
}
\arguments{
\item{titre}{A matrix of titres}

\item{proportion}{The proportion of titres to be removed}

\item{keep_homologous}{Whether to retain homologous titres}

\item{min_dim}{Integer dimensionality (\eqn{d \geq 1}) or \code{NULL}
(default).  When \code{NULL} a warning is issued if the result appears
underconstrained for a 2-dimensional map.  When set, removal is capped so
that at least \eqn{(n_\text{ag} + n_\text{sr}) d - d(d+1)/2} observations
remain, and a warning is issued if capping was needed or if the input is
already underconstrained.}
}
\value{
list
}
\description{
Generates a titre table with missing data
}
\examples{
m <- map_maker_random(5, 5, 10)
ti <- dist_to_hi_titre(m$dist)
miss_titres_random(ti$lessthan_titre, 0.2)
}
