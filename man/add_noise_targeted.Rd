% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_noise.R
\name{add_noise_targeted}
\alias{add_noise_targeted}
\title{Add noise to a specific titre, antigen row, or serum column}
\usage{
add_noise_targeted(
  dists,
  antigen = NULL,
  serum = NULL,
  rdistribution = stats::rnorm,
  noise_param = c(0, 1),
  seed
)
}
\arguments{
\item{dists}{A matrix of antigenic distances (as returned by \code{map_maker_random} or \code{map_maker_coord})}

\item{antigen}{Antigen name (character) or row index (integer). NULL to target all antigens for the given serum.}

\item{serum}{Serum name (character) or column index (integer). NULL to target all sera for the given antigen.}

\item{rdistribution}{Random distribution function for the noise}

\item{noise_param}{Arguments for the rdistribution function}

\item{seed}{Random seed}
}
\value{
list with dist_table, noise, noise_dist_table, and params
}
\description{
Adds noise to a single titre (specified by antigen and serum), an entire antigen row, or an entire serum column.
Antigens and sera can be identified by name or by integer index within the antigen×serum submatrix.
}
\examples{
m <- map_maker_random(5, 5, 10)
# Noise on a single titre
add_noise_targeted(m$dist, antigen = "AG1", serum = "SR2")
# Noise on all titres for antigen 1
add_noise_targeted(m$dist, antigen = 1)
# Noise on all titres for serum "SR3"
add_noise_targeted(m$dist, serum = "SR3")
}
