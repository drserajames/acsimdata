% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_titres_ext.R
\name{miss_titres_block}
\alias{miss_titres_block}
\title{Remove a rectangular block of titres}
\usage{
miss_titres_block(
  titre,
  antigens,
  sera,
  keep_homologous = TRUE,
  min_dim = NULL
)
}
\arguments{
\item{titre}{A character matrix of titres.}

\item{antigens}{Antigens to include in the block.  Either a character vector
of antigen names (matching \code{rownames(titre)}) or an integer vector of
row indices.}

\item{sera}{Sera to include in the block.  Either a character vector of serum
names (matching \code{colnames(titre)}) or an integer vector of column
indices.}

\item{keep_homologous}{Whether to protect homologous titres within the block
from removal.  Default \code{TRUE}.}

\item{min_dim}{Integer dimensionality (\eqn{d \geq 1}) or \code{NULL}
(default).  When \code{NULL} a warning is issued if the result appears
underconstrained for a 2-dimensional map.  When set, removal is capped so
that at least \eqn{(n_\text{ag} + n_\text{sr}) d - d(d+1)/2} observations
remain, and a warning is issued if capping was needed or if the input is
already underconstrained.}
}
\value{
A list with the same structure as \code{\link{miss_titres_random}}:
\code{full_titre}, \code{rm_titre}, \code{rm_ind}, \code{rm_ind_arr},
\code{params}.
}
\description{
Marks all titres within a specified antigen × serum subblock as missing
(\code{"*"}), simulating structured gaps such as those arising when certain
antigen panels are never tested against certain serum cohorts (e.g. titres
between geographically separate surveillance programmes).
}
\details{
Both \code{antigens} and \code{sera} must be provided; specifying only one
would be equivalent to removing entire rows or columns (use
\code{\link{miss_titres_random}} with \code{proportion = 1} on a subset if
that is what you need).
}
\examples{
m <- map_maker_random(5, 5, 10, seed = 1)
ti <- dist_to_hi_titre(m$dist)
# Remove the top-left 2x3 block
miss_titres_block(ti$lessthan_titre, antigens = 1:2, sera = 1:3)
# Or specify by name
miss_titres_block(ti$lessthan_titre, antigens = c("AG1","AG3"), sera = c("SR2","SR4"))
}
